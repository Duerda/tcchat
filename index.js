function Login() {
  window.location.href = "/Inicial-tela/Login/Log-aluno.html";
}
function Cadastrar() {
  window.location.href = "/Inicial-tela/Cadastro/Cad.html";
}

/* Teste de navegação da pagina
// Aguarda toda a página (HTML) carregar para executar o código
document.addEventListener('DOMContentLoaded', () => {
    

    const buttonInicio = document.getElementById('btnInicio');
    const buttonSobre = document.getElementById('btnSobre');
    const buttonIntegrantes = document.getElementById('btnIntegrantes');
    const buttonProgresso = document.getElementById('btnProgresso');

    if (buttonInicio) {
        buttonInicio.addEventListener('click', () => {
            window.scrollTo({ top: 0, behavior: "smooth" });
        });
    }

    if (buttonSobre) {
        buttonSobre.addEventListener('click', () => {
            window.scrollTo({ top: 2425, behavior: "smooth" }); 
        });
    }

    if (buttonIntegrantes) {
        buttonIntegrantes.addEventListener('click', () => {
            window.scrollTo({ top: 4490, behavior: "smooth" });
        });
    }

    if (buttonProgresso) {
        buttonProgresso.addEventListener('click', () => {
            window.scrollTo({ top: 0, behavior: "smooth" });
        });
    }
});
*/
